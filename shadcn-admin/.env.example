# Base URL của backend API. Build-time variable — Vite nướng vào bundle.
# Coolify/Prod: set domain công khai của API (vd: https://api.example.com/api/v1)
# Dev: để /api/v1 và để reverse proxy chuyển /api → api:3000.
VITE_API_URL=/api/v1
